<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
  <meta name="theme-color" content="#070b12">
  <meta name="description" content="ALIEN social deduction game">
  <title>ALIEN — Social Deduction Game</title>

  <!-- Supabase Realtime is used by alien_game.js for Online Mode. -->
  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>

  <style>
    :root{
      --bg:#070b12;
      --panel:#101722;
      --panel2:#0b111a;
      --line:#263243;
      --text:#f4f7fb;
      --muted:#9ba8ba;
      --accent:#6ea8ff;
      --accent2:#8c7cff;
      --danger:#ff5268;
      --success:#58d68d;
      --warning:#ffd166;
    }

    *{box-sizing:border-box}

    html,body{
      margin:0;
      min-height:100%;
      background:var(--bg);
      color:var(--text);
      font-family:system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,Arial,sans-serif;
      -webkit-text-size-adjust:100%;
    }

    body{
      min-height:100vh;
      min-height:100dvh;
      overflow-x:hidden;
    }

    button,input,select{font:inherit}

    button{
      appearance:none;
      -webkit-appearance:none;
      border:1px solid var(--line);
      border-radius:12px;
      background:#182231;
      color:var(--text);
      padding:14px 16px;
      min-height:48px;
      font-weight:800;
      cursor:pointer;
      touch-action:manipulation;
      user-select:none;
      -webkit-tap-highlight-color:transparent;
    }

    button:hover{filter:brightness(1.1)}
    button:active{transform:translateY(1px)}
    button:disabled{opacity:.45;cursor:not-allowed;filter:none}

    button.primary{
      background:var(--accent);
      border-color:var(--accent);
      color:#07101d;
    }

    button.secondary{
      background:#121b28;
    }

    button.full{width:100%}

    input,select{
      width:100%;
      min-height:48px;
      border:1px solid var(--line);
      border-radius:12px;
      background:#0b111a;
      color:var(--text);
      padding:12px 13px;
      outline:none;
    }

    input:focus,select:focus{
      border-color:var(--accent);
      box-shadow:0 0 0 3px rgba(110,168,255,.15);
    }

    .screen{
      display:none;
      width:100%;
      min-height:100vh;
      min-height:100dvh;
      padding:20px;
      align-items:center;
      justify-content:center;
    }

    .screen.active{display:flex}

    .panel{
      width:min(760px,100%);
      background:var(--panel);
      border:1px solid var(--line);
      border-radius:20px;
      padding:24px;
      box-shadow:0 18px 60px rgba(0,0,0,.28);
    }

    h1,h2,h3,p{margin-top:0}
    h1{font-size:clamp(28px,7vw,48px);line-height:1.05;margin-bottom:12px}
    h2{font-size:24px}
    h3{margin:22px 0 10px}
    p{line-height:1.55}

    .eyebrow{
      color:var(--muted);
      font-size:12px;
      font-weight:900;
      letter-spacing:1.4px;
      text-transform:uppercase;
      margin-bottom:9px;
    }

    .muted{color:var(--muted)}

    .setup-grid{
      display:grid;
      grid-template-columns:1fr 1fr;
      gap:10px;
      margin:16px 0;
    }

    .setup-player{
      display:grid;
      gap:10px;
      padding:14px;
      margin:10px 0;
      background:var(--panel2);
      border:1px solid var(--line);
      border-radius:14px;
    }

    .setup-player label,
    .custom-row label{
      display:grid;
      gap:6px;
      color:var(--muted);
      font-size:12px;
      font-weight:800;
      text-transform:uppercase;
    }

    .setup-player .player-name-input{
      text-transform:none;
    }

    .role-select.random-hidden{
      border-color:var(--accent2);
    }

    #playersSetup{
      display:grid;
      gap:2px;
    }

    #playerValidity{
      color:var(--muted);
      font-size:13px;
      font-weight:800;
      margin:12px 0;
    }

    .button-stack{
      display:grid;
      gap:10px;
      margin-top:14px;
    }

    .choice-grid{
      display:grid;
      grid-template-columns:repeat(2,minmax(0,1fr));
      gap:10px;
      margin-top:16px;
    }

    .choice-button{
      min-height:54px;
      width:100%;
      font-weight:900;
    }

    .choice-button.selected{
      outline:3px solid var(--accent);
      background:#243653;
    }

    .role-icon{
      font-size:72px;
      line-height:1;
      margin:10px 0 18px;
    }

    .role-title{
      font-size:32px;
      margin-bottom:8px;
    }

    .role-description,.role-description p{
      line-height:1.65;
    }

    .team-badge{
      display:inline-block;
      padding:7px 10px;
      border-radius:999px;
      background:#263243;
      color:var(--text);
      font-size:12px;
      font-weight:900;
      letter-spacing:.7px;
      margin-bottom:14px;
    }

    .human{color:var(--success)}
    .hostile{color:var(--danger)}
    .neutral{color:var(--warning)}
    .infection{color:#d18cff}

    .ally-box{
      margin-top:16px;
      padding:14px;
      border:1px solid var(--line);
      border-radius:14px;
      background:#0b111a;
      line-height:1.7;
    }

    .warning{
      color:var(--warning);
      font-weight:800;
    }

    .results-box,
    #roundResults,
    #systemsList,
    #finalPlayers{
      background:#0a1019;
      border:1px solid var(--line);
      border-radius:14px;
      padding:15px;
      line-height:1.8;
    }

    #finalPlayers{
      display:grid;
      gap:8px;
    }

    #finalPlayers .dead{
      opacity:.55;
      text-decoration:line-through;
    }

    .team-human{color:var(--success)}
    .team-hostile{color:var(--danger)}
    .team-neutral{color:var(--warning)}
    .team-infection{color:#d18cff}

    .modal{
      display:none;
      position:fixed;
      inset:0;
      z-index:1000;
      padding:16px;
      background:rgba(0,0,0,.72);
      overflow:auto;
      align-items:center;
      justify-content:center;
    }

    .modal.open{display:flex}

    .modal-card{
      width:min(760px,100%);
      max-height:92vh;
      overflow:auto;
      background:var(--panel);
      border:1px solid var(--line);
      border-radius:18px;
      padding:20px;
    }

    .modal-head{
      display:flex;
      justify-content:space-between;
      align-items:center;
      gap:12px;
      margin-bottom:8px;
    }

    .modal-close{
      min-width:48px;
      padding:10px;
    }

    .guide-card{
      display:grid;
      grid-template-columns:auto 1fr;
      gap:13px;
      padding:14px;
      margin:9px 0;
      border:1px solid var(--line);
      border-radius:14px;
      background:#0b111a;
    }

    .guide-icon{
      font-size:30px;
      line-height:1;
    }

    .guide-team{
      color:var(--muted);
      font-size:12px;
      font-weight:900;
      text-transform:uppercase;
      margin-top:2px;
    }

    .guide-card p{
      color:var(--muted);
      margin:7px 0 0;
    }

    .custom-row{
      display:grid;
      grid-template-columns:1fr 110px auto;
      gap:10px;
      align-items:center;
      padding:12px 0;
      border-bottom:1px solid var(--line);
    }

    .custom-row.locked{opacity:.7}

    .switch{
      display:flex!important;
      align-items:center;
      gap:8px;
      color:var(--text)!important;
    }

    .switch input{
      width:20px;
      min-height:20px;
      accent-color:var(--accent);
    }

    @media(max-width:600px){
      .screen{padding:12px}
      .panel{padding:18px;border-radius:16px}
      .setup-grid{grid-template-columns:1fr}
      .choice-grid{grid-template-columns:1fr}
      .custom-row{grid-template-columns:1fr}
      h1{font-size:32px}
      .role-icon{font-size:58px}
    }
  </style>
</head>

<body>

  <!-- =========================================================
       LOCAL GAME SCREENS
       alien_game.js expects these IDs.
       ========================================================= -->

  <main id="setupScreen" class="screen active">
    <div class="panel">
      <div class="eyebrow">👽 ALIEN</div>
      <h1>GAME SETUP</h1>
      <p class="muted">Set up your crew, choose roles, then start the game.</p>

      <label>
        <span class="eyebrow">PLAYER COUNT</span>
        <select id="playerCount">
          <option value="4">4 PLAYERS</option>
          <option value="5">5 PLAYERS</option>
          <option value="6">6 PLAYERS</option>
          <option value="7">7 PLAYERS</option>
          <option value="8">8 PLAYERS</option>
          <option value="9">9 PLAYERS</option>
          <option value="10">10 PLAYERS</option>
          <option value="11">11 PLAYERS</option>
          <option value="12">12 PLAYERS</option>
        </select>
      </label>

      <div class="button-stack">
        <button id="randomRolesButton" type="button" class="primary">🎲 RANDOMISE ROLES</button>
      </div>

      <div id="playerValidity"></div>
      <div id="playersSetup"></div>

      <div class="button-stack">
        <button id="roleGuideButton" type="button">📖 ROLE GUIDE</button>
        <button id="customRolesButton" type="button">⚙️ CUSTOM ROLES</button>
        <button id="startGameButton" type="button" class="primary">🚀 START GAME</button>
        <button id="openOnlineButton" type="button" class="secondary">🌐 ONLINE MODE</button>
      </div>
    </div>
  </main>

  <section id="passScreen" class="screen">
    <div class="panel">
      <div class="eyebrow" id="passRound">ROUND 1 • STAGE 1 / 10</div>
      <h1>PASS THE PHONE</h1>
      <p id="passSubtext" class="muted">PASS THE PHONE TO THIS PLAYER</p>
      <h2 id="passPlayerName">Player 1</h2>
      <button id="readyButton" type="button" class="primary full">I'M READY</button>
    </div>
  </section>

  <section id="roleScreen" class="screen">
    <div class="panel">
      <div class="eyebrow">YOUR SECRET ROLE</div>
      <div id="roleIcon" class="role-icon">❓</div>
      <h1 id="roleName" class="role-title">Role</h1>
      <div id="roleTeam" class="team-badge">HUMAN TEAM</div>
      <p id="rolePlayerName" class="muted"></p>
      <p id="roleDescription"></p>
      <div id="hostileList"></div>
      <button id="showActionButton" type="button" class="primary full">SHOW ACTION</button>
    </div>
  </section>

  <section id="actionScreen" class="screen">
    <div class="panel">
      <div class="eyebrow">YOUR ABILITY</div>
      <h1 id="actionTitle">ABILITY</h1>
      <p id="actionDescription" class="muted"></p>
      <div id="actionOptions" class="choice-grid"></div>
      <button id="confirmActionButton" type="button" class="primary full">CONFIRM</button>
    </div>
  </section>

  <section id="reactionScreen" class="screen">
    <div class="panel">
      <div class="eyebrow" id="reactionRound">ROUND 1</div>
      <p id="reactionStage" class="muted">STAGE 1 / 10</p>
      <h1 id="reactionPlayerName">Player 1</h1>
      <button id="reactionReadyButton" type="button" class="primary full">SHOW MY RESULT</button>
    </div>
  </section>

  <section id="reactionResultScreen" class="screen">
    <div class="panel">
      <div class="eyebrow">PRIVATE RESULT</div>
      <h1 id="reactionResultTitle">ROUND RESULT</h1>
      <p id="reactionResultMessage" class="large-message"></p>
      <button id="reactionContinueButton" type="button" class="primary full">CONTINUE</button>
    </div>
  </section>

  <section id="discussionScreen" class="screen">
    <div class="panel">
      <div class="eyebrow" id="discussionRound">ROUND 1</div>
      <p id="discussionStage" class="muted">STAGE 1 / 10</p>
      <h1>DISCUSSION</h1>
      <div id="roundResults"></div>
      <button id="startVotingButton" type="button" class="primary full" style="margin-top:14px">START VOTING</button>
    </div>
  </section>

  <section id="votingScreen" class="screen">
    <div class="panel">
      <div class="eyebrow" id="votingRound">ROUND 1</div>
      <p id="votingStage" class="muted">STAGE 1 / 10</p>
      <h1>YOUR VOTE</h1>
      <h2 id="voterName">Player 1</h2>
      <p id="votingSilenced" class="warning"></p>
      <div id="voteOptions" class="choice-grid"></div>
      <button id="confirmVoteButton" type="button" class="primary full">CONFIRM VOTE</button>
    </div>
  </section>

  <section id="captainTieScreen" class="screen">
    <div class="panel">
      <div class="eyebrow">CAPTAIN</div>
      <h1>VOTE TIE</h1>
      <div id="captainTieOptions" class="choice-grid"></div>
    </div>
  </section>

  <section id="judgeScreen" class="screen">
    <div class="panel">
      <div class="eyebrow">⚖️ JUDGE</div>
      <h1>JUDGE DECISION</h1>
      <p id="judgeDescription"></p>
      <div class="choice-grid">
        <button id="judgeCancelButton" type="button" class="primary">CANCEL EJECTION</button>
        <button id="judgeAllowButton" type="button">ALLOW EJECTION</button>
      </div>
    </div>
  </section>

  <section id="voteResultScreen" class="screen">
    <div class="panel">
      <div class="eyebrow">VOTE RESULT</div>
      <h1 id="voteResultTitle">NO EJECTION</h1>
      <p id="voteResultMessage"></p>
      <button id="afterVoteButton" type="button" class="primary full">CONTINUE</button>
    </div>
  </section>

  <section id="lifelineScreen" class="screen">
    <div class="panel">
      <div class="eyebrow">📡 EARTH TRANSMISSION</div>
      <h1 id="lifelineTitle">EARTH LIFELINE</h1>
      <p id="lifelineMessage" class="large-message"></p>
      <button id="lifelineContinue" type="button" class="primary full">CONTINUE</button>
    </div>
  </section>

  <section id="systemsScreen" class="screen">
    <div class="panel">
      <div class="eyebrow" id="systemsRound">ROUND 1</div>
      <p id="systemsStage" class="muted">STAGE 1 / 10</p>
      <h1>SHIP SYSTEMS</h1>
      <div id="systemsList"></div>
      <button id="nextRoundButton" type="button" class="primary full" style="margin-top:14px">NEXT ROUND</button>
    </div>
  </section>

  <section id="gameOverScreen" class="screen">
    <div class="panel">
      <div class="eyebrow">GAME OVER</div>
      <h1 id="gameOverTitle">GAME OVER</h1>
      <p id="gameOverMessage"></p>
      <h2>FINAL PLAYERS</h2>
      <div id="finalPlayers"></div>
      <button id="restartButton" type="button" class="primary full" style="margin-top:16px">PLAY AGAIN</button>
    </div>
  </section>

  <!-- =========================================================
       MODALS
       ========================================================= -->

  <div id="roleGuideModal" class="modal" aria-hidden="true">
    <div class="modal-card">
      <div class="modal-head">
        <div>
          <div class="eyebrow">ALIEN</div>
          <h2>ROLE GUIDE</h2>
        </div>
        <button type="button" class="modal-close" data-close="roleGuideModal" aria-label="Close">✕</button>
      </div>
      <div id="roleGuideContent"></div>
    </div>
  </div>

  <div id="customRoleModal" class="modal" aria-hidden="true">
    <div class="modal-card">
      <div class="modal-head">
        <div>
          <div class="eyebrow">ALIEN</div>
          <h2>CUSTOM ROLES</h2>
        </div>
        <button type="button" class="modal-close" data-close="customRoleModal" aria-label="Close">✕</button>
      </div>
      <div id="customRoleContent"></div>
      <button id="applyCustomRolesButton" type="button" class="primary full" style="margin-top:16px">APPLY CUSTOM ROLES</button>
    </div>
  </div>

  <!-- alien_game.js creates the Online Mode screens itself. -->
  <script src="alien_game.js"></script>
</body>
</html>
